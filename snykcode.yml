version: 1

# Enable Rust explicitly (recommended)
languages:
  rust:
    enabled: true

# Include only application-relevant code
include:
  - src/**

# Exclude noisy sources
exclude:
  - target/**
  - tests/**
  - benches/**
# Optional: Enable deep semantic checks
experimental:
  rust_dataflow: true
